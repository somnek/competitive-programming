def word_mesh(words):
	final_string = ''
	for i in range(len(words) - 1):
		print(f"first word: {f_word}\nnext word: {n_word}")
		f_word = words[i]
		n_word = words[i + 1]
		for j in reversed(list(x for x in range(len(f_word)))):

    
    """NOT FINISH"""

		
	return final_string


print(word_mesh(["beacon", "condominium", "umbilical", "california"]))
#"conumcal"
